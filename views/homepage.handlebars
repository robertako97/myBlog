{{#each posts as |post|}}
    <div class="row align-center mb-5">
        <div class="col-md-6">
            <h2>{{post.title}}</h2>
            <p>{{post.post_content}}</p>
        </div>
    </div>
{{/each}}