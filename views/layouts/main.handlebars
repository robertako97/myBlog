<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />

    <title>My Blog!</title>
    <link rel="stylesheet" href="/css/style.css">

    <style>
        /* Center-align the title */
        .space{
        padding-bottom: 20px;
        padding-top: 20px;

        }

        .centered-title {
            text-align: center;
            margin: 0; /* Remove any default margin */
        }

        /* Style the title */
        .centered-title a {
            text-decoration: none;
            font-size: 36px; /* Adjust the font size */
            color: #333; /* Dark gray color */
            transition: color 0.3s; /* Smooth color transition on hover */
        }

        .centered-title a:hover {
            color: #007bff; /* Blue color on hover */
        }

        /* Style the buttons */
        .nav-buttons a {
            text-decoration: none;
            color: #007bff; /* Blue color */
            font-size: 18px; /* Adjust the font size */
            margin: 0 10px;
            padding: 5px 10px; /* Add padding for better clickability */
            border: 1px solid #007bff; /* Add a border */
            border-radius: 3px; /* Rounded corners */
            transition: background-color 0.3s, color 0.3s; /* Smooth color and background transitions on hover */
        }

        .nav-buttons a:hover {
            background-color: #007bff; /* Blue background on hover */
            color: #fff; /* White text on hover */
        }
        .button-like-link {
            padding-left:5px;
            background: none;
            border: none;
            color: #007bff; /* Blue color similar to the hyperlink */
            text-decoration: underline; /* Underline text like a hyperlink */
            cursor: pointer;
            font-size: inherit; /* Inherit font size */
            font-family: inherit; /* Inherit font family */
            /* Add any other styles to match your hyperlink styling */
        }

        .button-like-link:hover {
            text-decoration: none; /* Remove underline on hover if needed */
            color: #0056b3; /* Change color on hover */
        }

        .container {
            overflow: hidden;
        }

        /* Image styles */
        .container img {
            max-width: 100%;
            height: auto;
            transition: opacity 1.3s ease-in-out;
        }

        /* Hover effect */
        .container:hover img {
            opacity: 0;
            transform: scale(1.1);
        }
        .radius {
            border-radius: 100px;
        }


    </style>

</head>

<body>
<div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center centered-title">
        <h1 class="centered-title"> <!-- Apply the centered-title class -->
            <h1 class="centered-title space">iDea</h1>
            </h1>
            <span class="container" style="display: flex; justify-content: center; align-items: center; padding-bottom: 25px;">
        <img class="radius" src="https://logodix.com/logo/1311419.jpg" alt="Image Description" width="100" height="100">
    </a>
</span>
            <nav class="nav-buttons centered-title"> <!-- Apply the nav-buttons class -->
                <!-- Conditionally render login or logout links -->
                {{#if logged_in}}
                    <a href="/">Home</a>
                    <a href="/dashboard">Dashboard</a>
                    <a href="/login">Login</a>
                    <button class="button-like-link no-button" id="logout">logout</button>

                {{else}}
                    <a href="/login">login</a>
                {{/if}}
            </nav>
    </header>
    <main class="container container-fluid mt-5">
        <!-- Render the sub layout -->
        {{{ body }}}
    </main>
    <footer>

    </footer>
</div>

<!-- Render script for logged-in users only -->

{{#if logged_in}}
    <script src="/js/logout.js"></script>
    <script src="/js/buttons.js"></script>

{{/if}}
</body>

</html>


